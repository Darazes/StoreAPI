@model IEnumerable<StoreAPI.Models.Storage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.adress_storage)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.adress_storage)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_storage }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_storage }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_storage })
        </td>
    </tr>
}

</table>
