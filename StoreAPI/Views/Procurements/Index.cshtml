@model IEnumerable<StoreAPI.Models.Procurement>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.date_procurement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cost_procurement)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.count_procurement)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.date_procurement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.cost_procurement)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.count_procurement)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_procurement }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_procurement }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_procurement })
        </td>
    </tr>
}

</table>
